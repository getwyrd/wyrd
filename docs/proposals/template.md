# Proposal: <title>

- Status: draft | accepted | implemented | withdrawn
- Author(s):
- Created:
- Tracking issue:

> Enhancement proposals (KEP/RFC-style) are how architectural change flows once
> something is implemented. Move this file from `draft/` to `accepted/` when
> accepted. Early in the project this is deliberately lightweight; the discipline
> matters more than the ceremony. If a decision is foundational rather than an
> enhancement, it may instead warrant an ADR.

## Motivation

What problem does this solve? Why now? What is the user- or operator-visible need?

## Design

The proposed change. Be concrete about interfaces, the affected layers (L1–L5),
and any changes to the commit protocol, the consistency contract, or the on-disk
format (the latter two carry the highest bar — see ADR-0015 and ADR-0002).

## Alternatives considered

What else was weighed, and why this over those?

## Graduation criteria

How do we know it is done and correct? What tests (including DST / fault
injection — ADR-0009) and what definition of done? What telemetry confirms it in
production (ADR-0011)?

## Backward compatibility

Effect on existing data (on-disk format), existing deployments (version skew —
the fleet is half-upgraded during rollout), and the public API.

## Open questions

Mark unresolved points explicitly.
